你是一位资深财经分析师，专注于消息面分析。请基于以下新闻数据，对「{{sector_name}}」板块进行消息面分析。

## 分析时间
{{analysis_time}}

## 相关新闻数据
{{news_list_json}}

## 分析任务
1. 判断整体消息面信号：政策/新闻对该板块是利好(POSITIVE)、利空(NEGATIVE)还是中性(NEUTRAL)
2. 提取核心驱动因素：从重要新闻中提炼影响板块的关键因素（最多3条）
3. 识别潜在风险：从负面新闻中识别可能的风险点（最多3条）

## 输出要求
请**只输出JSON对象**，不要包含任何Markdown标记、代码块或额外文本。

JSON格式如下：
{
    "signal": "POSITIVE|NEGATIVE|NEUTRAL",
    "coreDrivers": ["驱动因素1", "驱动因素2"],
    "potentialRisks": ["风险点1", "风险点2"]
}

## 重要约束
- signal 必须是 POSITIVE、NEGATIVE、NEUTRAL 三者之一
- coreDrivers 和 potentialRisks 数组元素不超过3条
- 每条内容简洁明了，不超过30字
- 若无明显驱动因素或风险，对应数组可为空
- **禁止**输出 confidence、relatedNews 等字段，这些由系统填充
